{
  "name": "mapviewer-sample",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "build": "node scripts/build.mjs",
    "check": "npm run test && npm run build"
  },
  "devDependencies": {
    "vitest": "^2.0.0"
  }
}
